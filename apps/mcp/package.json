{
  "name": "mcp",
  "version": "1.4.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "bin": {
    "asobeast-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src",
    "wire-check": "node --test --test-reporter=spec scripts/wire-check.mjs"
  },
  "dependencies": {
    "@asobeast/mcp-tools": "workspace:*",
    "@asobeast/shared": "workspace:*",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@asobeast/typescript-config": "workspace:*",
    "@eslint/js": "^10.0.1",
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^26.5.1",
    "eslint": "^10.10.0",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.0"
  }
}
